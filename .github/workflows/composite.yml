name: composite action test

on:
  push:

jobs:
  composite-action:
    name: job to test a compsite action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        id: compositeAction
        with:
          number1: 5
          number2: 7
      - run: echo ${{ steps.compositeAction.outputs.result }}
    
